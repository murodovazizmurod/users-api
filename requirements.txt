# Runtime dependencies.
# Lower bounds rather than exact pins so the project installs on any modern
# CPython (3.12+). The Docker image pins the interpreter, which is what makes
# a deployment reproducible.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
email-validator>=2.2,<3.0

# Persistence
SQLAlchemy[asyncio]>=2.0.36,<3.0
alembic>=1.14,<2.0
asyncpg>=0.30
aiosqlite>=0.20

# Security
PyJWT>=2.10,<3.0
argon2-cffi>=23.1

# Background jobs and shared rate-limit counters
celery[redis]>=5.4,<6.0
redis>=5.0
